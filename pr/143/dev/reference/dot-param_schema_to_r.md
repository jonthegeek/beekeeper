# Map parameter schemas to `stbl` helpers

Map parameter schemas to `stbl` helpers

## Usage

``` r
.param_schema_to_r(params_schema)
```

## Arguments

- params_schema:

  (`data.frame`) Schema metadata for operation parameters.

## Value

(`character`) Coercion helper names.
