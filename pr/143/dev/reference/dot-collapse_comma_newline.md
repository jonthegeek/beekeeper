# Collapse to a comma-separated vertical string

Collapse to a comma-separated vertical string

## Usage

``` r
.collapse_comma_newline(to_collapse)
```

## Arguments

- to_collapse:

  (`character`) The character vector to collapse.

## Value

(`glue`) A length-1, comma-separated glue object with newlines.
