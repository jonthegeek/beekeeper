# Format a subset_path vector as an R string literal

Format a subset_path vector as an R string literal

## Usage

``` r
.format_subset_path_str(subset_path)
```

## Value

(`character(1)`) Either `'"name"'` or `'c("a", "b")'`.
