# Convert a path to a request argument

Convert a path to a request argument

## Usage

``` r
.path_as_arg(path, params_df)
```

## Arguments

- path:

  (`character(1)`) The path template.

- params_df:

  (`data.frame`) Parameter metadata in tabular form.

## Value

(`character(1)`) Code for the request path argument.
