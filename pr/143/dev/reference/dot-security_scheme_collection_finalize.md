# Finalize collected security scheme data

Finalize collected security scheme data

## Usage

``` r
.security_scheme_collection_finalize(security_scheme_collection)
```

## Arguments

- security_scheme_collection:

  (`list`) Collected security scheme metadata.

## Value

(`list`) Finalized security metadata.
