# Prepare parameter metadata

Prepare parameter metadata

## Usage

``` r
.prepare_params_df(params_df)
```

## Arguments

- params_df:

  (`data.frame`) Parameter metadata in tabular form.

## Value

(`data.frame`) Prepared parameter metadata.
