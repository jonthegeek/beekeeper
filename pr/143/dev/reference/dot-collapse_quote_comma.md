# Collapse to a comma-separated quoted string

Collapse to a comma-separated quoted string

## Usage

``` r
.collapse_quote_comma(to_collapse)
```

## Arguments

- to_collapse:

  (`character`) The character vector to collapse.

## Value

(`glue`) A length-1, comma-separated glue object.
