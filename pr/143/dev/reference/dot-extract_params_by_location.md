# Extract parameter names by location

Extract parameter names by location

## Usage

``` r
.extract_params_by_location(params_df, filter_in)
```

## Arguments

- params_df:

  (`data.frame`) Parameter metadata in tabular form.

- filter_in:

  (`character(1)`) The parameter location to keep.

## Value

(`character`) Parameter names for the requested location.
