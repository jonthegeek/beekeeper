# Convert parameters to a signature string

Convert parameters to a signature string

## Usage

``` r
.params_to_args(params)
```

## Arguments

- params:

  (`list`) Parameter metadata for a single operation.

## Value

(`character(1)`) A comma-separated parameter string.
