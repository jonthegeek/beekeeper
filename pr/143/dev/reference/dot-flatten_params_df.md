# Flatten parameter metadata

Flatten parameter metadata

## Usage

``` r
.flatten_params_df(params_df)
```

## Arguments

- params_df:

  (`data.frame`) Parameter metadata in tabular form.

## Value

(`data.frame`) Flattened parameter metadata.
