# Stabilize beekeeper config values

Stabilize beekeeper config values

## Usage

``` r
.stabilize_config(config)
```

## Arguments

- config:

  (`list`) Package-generation configuration data.

## Value

(`list`) A stabilized config list.
